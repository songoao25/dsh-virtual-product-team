# 迭代路线图：产品团队模式（dsh-virtual-product-team）

> 日期：2026-08-16
> 阶段 8（迭代与维护，环节⑫）交付物。G8 通过后回环开发。

## 反馈池

| 反馈 | 来源 | 优先级 | 建议方案 |
|---|---|---|---|
| "开发 DSH 运行模式/插件类产品时，产品团队模式没有创造模式那套自我改造工具，够不够用？" → 用户拍板：全面升级，连自我改造工具一起加 | 用户对话（2026-08-16） | 高（已拍板） | 见"下一版本计划 v1.1.0" |
| 8 阶段架构上线后（01-idea-validation ~ 08-iteration），docs/AUDIT.md 仍是 6 阶段版（v1.0.0 审计），已过时 | 内部自检（2026-08-16） | 高 | v1.1.0 重新审计，覆盖 8 技能 + 新增能力 |

## 下一版本计划 v1.1.0

### 做（本次迭代范围）
1. **能力升级**：`agent.cordis.yml` 加入 `tool-cordis`（自我改造工具，来自官方创造模式同款配置）——使本模式可开发 DSH 模式/插件类产品
2. **技能补充**：将官方创造模式的两个技能复制进 `preset/skills/`：
   - `cordis-plugin-development`（怎么写 DSH 插件）
   - `editing-cordis-compositions`（怎么改运行组合/写新模式）
3. **persona 更新**：说明本模式现可开发"DSH 自身模式/插件"类产品，开发这类产品时加载上述技能；文件头注释同步
4. **同步安装副本**：`~/.dsh/.agent-presets/dsh-virtual-product-team/` 与仓库逐文件一致（diff 验证）
5. **文档更新**：CHANGELOG 增补 v1.1.0；README（中英）能力说明同步；AGENTS.md 技能清单更新
6. **重新审计**：按 8 阶段架构 + 新能力重写 docs/AUDIT.md（含安全边界提示：tool-cordis = 会话等同 shell 权限）

### 不做（明确排除）
- 可视化进度面板（仍留后续版本）
- 其他新功能、多语言本地化

## 安全边界说明（升级必须知晓）
- `tool-cordis` 是"信任边界而非沙箱"：使用该工具的会话拥有修改 DSH 运行系统的能力（与创造模式同等权限）
- 该能力仅在用户选择本模式并明确要求开发 DSH 类产品时使用；日常开发普通产品不受影响

## 回环
G8 通过（用户确认 v1.1.0 计划）→ 回阶段 4 开发（dev-qa）→ QA 重新审计 → 发布 v1.1.0 → 继续运营/迭代循环。
